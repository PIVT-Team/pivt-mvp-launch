# Lovable Paste Queue

Tracker for client-side fixes that exist on the `dev` branch but haven't been pasted into Lovable's editor yet. These changes only affect the **live demo URL** if you apply them in Lovable (Lovable commits them to `main` automatically).

The fixes are stable on `dev`. Pasting any of them is safe — each is a self-contained patch that doesn't depend on other dev-only changes. Apply at your discretion.

## Already pasted into Lovable (✅ live)

| Date | File | Commit on `dev` | Effect on live |
|---|---|---|---|
| 2026-05-13 | `supabase/functions/_shared/require-jwt.ts` | `8c70a56` | Replaced manual HS256 verification with `supabase.auth.getUser()`. Unblocked 10+ auth-requiring edge functions (document-ai, newton, manual-verify, etc.). |
| 2026-05-13 | `src/services/newtonActionService.ts` | `608b03c` | Send user JWT (not anon key) so `newton-action` accepts authenticated requests. Fixes the "Unknown error" / "Action needs attention" that appeared when clicking Newton chips (Generate KYC, list blockers, etc.). |

## Still queued — paste when ready

### ⭐ Test 2 — edge functions (4 files, order-dependent)

See **[LOVABLE_PASTE_TEST2.md](LOVABLE_PASTE_TEST2.md)** for the full guide. Summary:
paste `_shared/entity-match.ts` and `_shared/funds-flow-diff.ts` (both new) *before*
`deal-workflow-orchestrator/index.ts`, which imports them.
`discrepancy-engine/index.ts` is standalone and can go any time.

Both migrations are already applied. Without these four files the DB has the
versioning columns and triggers but nothing writes `deal_change_events`, so the
"what changed between funds flow versions" pipeline stays dark.


### Auth-bearer pattern (5 files)

Same root pattern as `newtonActionService.ts`. Five client files were sending the anon key as the Bearer token; the rewrite makes them send the user's session JWT (with anon as fallback). Each is one file, ~5-10 line change, self-contained.

| Priority | File | What it fixes |
|---|---|---|
| **High** if you use Newton chat | `src/components/newton/NewtonGlobalChat.tsx` | Global Newton chat panel (the floating sparkle button). Chat would fail or partially work without the user JWT. |
| **High** if you use Newton chat | `src/components/pivt-complete/cover/NewtonCover.tsx` | Newton cover section (full-page Newton view). Same chat function. |
| **Medium** | `src/components/pivt-complete/cover/NewtonAgentPanel.tsx` | Alternate Newton agent panel that also calls `newton-action`. Mirrors the fix already pasted in `newtonActionService.ts`. |
| **Medium** | `src/components/pivt-complete/cover/DocumentsCover.tsx` | Document Q&A panel — lets you ask Newton questions about uploaded docs. The "qa" action against `document-ai` would fail without the user JWT. |
| **Low** | `src/components/support/SupportPanel.tsx` | In-app support chat (the question mark / help button). Would fall back to anon, may still work via the chat function's lenient auth, but should send user JWT for consistency. |

### UX hardening (independent)

| Priority | File(s) | Commit | What it fixes |
|---|---|---|---|
| **Medium** | `src/components/pivt-complete/cover/DealsCover.tsx` + `src/hooks/useDealOperations.ts` | `952f61d` | Gates the "+ New Deal" button and "Create your first deal" CTA behind auth. Signed-out users now get redirected to `/login?next=…` instead of being allowed to fill the modal and then getting a cryptic Postgres RLS error on submit. Defense-in-depth toast in `createDeal` for any caller that bypasses the gating (Newton, Command Palette, etc.). |
| **Medium** | `src/components/pivt-complete/cover/ApprovalsWorkflowCover.tsx` | `0052930` | Fixes "Failed to add approver" on the Execution → Approvals tab. The `deal_approvals.approval_side` column has a CHECK constraint that only allows `'buyer'` or `'seller'`, but the UI was sending the richer UI role (e.g. "Seller Counsel", "Target Signatory") straight through, which the DB rejected. A `sideFromRole()` mapper now collapses any seller/target-ish role to `'seller'` and everything else to `'buyer'`. Also surfaces the real DB error message in the toast so future failures are diagnosable instead of showing the same opaque "Failed to add approver". |
| **Medium** | `src/components/pivt-complete/cover/ContactsDealTab.tsx` + `KycKybDealTab.tsx` + `VerificationReviewCover.tsx` | `4683f16` | Adds manual Approve / Reject controls across the three places a deal owner reviews people: Contacts tab (per-row Approve/Reject buttons + reject-reason dialog), KYB/KYC tab (the inline Review/Manual button now works for any non-verified row, not just `submitted`; review queue rows are now actionable too), and the Verification review queue cover (the inline Verify and the expanded Mark Verified / Mark Failed buttons were previously gated to platform admin via `isAdmin` — backend `manual-verify` only requires a valid JWT and RLS gates the underlying write, so the client gate was over-restrictive). |
| **Low** | `src/services/dealMetricsService.ts` | `1dc4bab` | Fixes the yellow dot next to "Stakeholders" in the workspace sidebar — it was wired to "every stakeholder verified" so it stayed yellow until KYC was 100% done, even after the roster was complete. Verification already has its own sidebar dot just below, so KYC was being tracked twice. New rule: stakeholders dot is green once buyer side + seller side are both present (`stakeholdersConfigured`); KYC progress continues to drive the verification dot. |
| **Low** | `src/services/dealMetricsService.ts` | `63f17b6` | Fixes the yellow dot next to "Deal Inputs" — it was gated on satisfying all 8 categoryChecks (financial + cap_table + waterfall + wire_instructions + tax + contracts + governance + obligations). The bar was so high that even the test's "Clean Deal — Healthy Close" scenario failed it (3/8 missing), which is why the test deliberately doesn't assert `deal_inputs` is complete. The non-required categories (waterfall, wires, tax, governance, obligations) are already gated under the Execution dot. New rule: Deal Inputs is complete when `cap_table` and `contracts` are both present (stakeholders + a core contract uploaded). |
| **Medium** | `src/services/dealMetricsService.ts` + `src/components/pivt-complete/cover/ApprovalsWorkflowCover.tsx` | `f349661` | Fixes two bugs on the Approvals tab: (a) the sidebar dot was blank because `stageStatuses` had no `approvals` key — `getStepDot('approvals')` always read undefined and fell through to grey. Added the key with a rule that turns green when every approval is granted (handles the all-optional case too), blocked when any row has a declined/expired/failed/blocked status, in_progress otherwise. (b) Added manual Approve / Decline buttons on every actionable row. Approve writes `status='approved' + completed_at`; Decline opens a reason dialog and writes `status='rejected' + declined_at + blocker_reason`. Both append to `audit_log`. Uses the two CHECK-constraint-safe values; `mapDbStatus` translates them to the UI's completed/declined badges. |
| **Low** | `src/components/pivt-complete/cover/DealWorkspaceCover.tsx` | `55a8be9` | Hover any sidebar dot to see what's behind the color. Tooltips show per-stage progress counts (e.g. "Approvals — In progress · 2 of 5 required granted (3/7 total)") so users can tell at a glance what's left without opening the tab. Steps without measurable progress (Overview / Audit / Comments) skip the tooltip. The dot trigger is wrapped in a padded span so the 6px dot has a real hover target. |
| **Medium** | `src/services/reportPersistenceService.ts` (new) + `GenerateReportModal.tsx` + `ReportsHistoryTable.tsx` + `DealReportsCover.tsx` | `0893f05` | Reports tab fixed two customer-readiness gaps without adding new schema. (1) Generated report blobs are now uploaded to the existing `deal-documents` bucket under a `reports/` prefix, and metadata is persisted to `audit_log` (action `report_generated`, with `report_deleted` tombstones for removals). Download now fetches a signed URL, so reports survive page refresh and are visible across devices/users. (2) The Schedule toggle is now functional — switching it on writes a `report_schedule_set` row to `audit_log` (latest row per `deal_id, report_type_id, format` wins), and an overdue banner appears at the top of the tab when any saved schedule is due. There's no server cron yet, so the prompt is opportunistic (fires when the user opens the tab) — honest substitute until cron is wired. |
| **Low** | `src/services/dealMetricsService.ts` + `DealWorkspaceCover.tsx` | `0a9bad1` | Fixes the blank dot next to "Audit" in the workspace sidebar — same root cause as the Approvals one (no `audit` key in `stageStatuses`). Rule: 0 audit_log events → not_started (grey), ≥1 → complete (green). Audit is a growing trail, not a checklist, so "complete" here means "the trail is live and being captured." Tooltip reads e.g. "Audit — Trail live · 47 events recorded". Adds an `audit_log` presence probe to the metrics fetch (single row, `.limit(1)`). |
| **Low** | `src/services/dealMetricsService.ts` + `DealWorkspaceCover.tsx` | `28a1316` | Fixes the remaining blank sidebar dots (Overview + Comments). Overview is the deal home view — turns green ("Deal loaded") whenever the deal record exists. Comments mirrors Audit's growing-trail model — 0 `deal_comments` rows → not_started, ≥1 → complete. Tooltip shows "N threads · M comments total." Adds a `deal_comments` presence probe (limit 200) plus `commentCount` + `commentThreadCount` on `DealMetrics`. |
| **Medium** | `src/components/pivt-complete/cover/EscrowCover.tsx` | `37afdf9` | Wires the two **Escrow** stubs found in the manual test. "Add Escrow Details" now opens a real modal (institution, FBO/Dedicated account type, status, masked account, interest rate) that upserts into `escrow_accounts` on `deal_id` (the table has UNIQUE(deal_id) so the same flow handles create + edit). "Upload Escrow Agreement" opens a file picker → uploads to the existing `deal-documents` bucket → inserts a `deal_documents` row tagged `doc_type='ESCROW_AGREEMENT'` so the EscrowCover query filters to escrow files. Both actions audit-log their events. Uploaded agreements list above the transactions table with click-through signed URLs. |
| **Low** | `src/components/pivt-complete/cover/PaymentVerificationCover.tsx` | `d3725c0` | Fixes the wire-rejection notes that were silently discarded. The Reject dialog's `resolutionNote` textarea was collected but never persisted (wire_instructions has no notes column). Now reason is required (Confirm disabled until non-empty), and the rejection is recorded in `audit_log` with action `wire_instruction_rejected` and details `{wire_id, payee, amount, reason}` — mirroring the verify side. Also renames dialog UI from "Flag for Review" to honest "Reject Wire Instruction" + "Confirm Reject". |
| **Low** | `src/components/pivt-complete/cover/CapTableCover.tsx` | `2858454` | Empty-state edge case: clicking "Add Equity Holder" in the no-entries state set `showAddRow=true` but the empty-state card kept rendering on top, so users saw stacked UIs and thought nothing happened. Adds `!showAddRow` to the empty-state guard so it cleanly hides when the inline add form is open. |
| **Low** | `src/components/pivt-complete/cover/AuditConsoleCover.tsx` | `74062d5` | Removes a React `validateDOMNesting` warning firing every time the Audit Log tab renders — the event-timeline row was a `<button>` wrapping an inner `<button>` for the record-navigation link. Converted the outer wrapper to a div with `role="button"`, `tabIndex`, Enter/Space keyboard handler, and `aria-expanded` so the row stays keyboard-accessible while the inner record link remains a real button. |
| **HIGH** | `src/services/disbursementService.ts` (new) + `WirePackCover.tsx` + `PaymentsCover.tsx` | `b3717df` | **Wires the "Execute Disbursement" flow end-to-end** — the actual close-the-deal action that was completely missing. The Wire Pack tab generated PDFs but had no Execute button anywhere, no `disbursement_intents` rows were ever created, and deals stayed in `active` forever. New `disbursementService` pulls verified wires, creates one `disbursement_intent` per wire, then runs a mock partner-bank simulator (eligible → executing → executed → settled) with `audit_log` writes at every transition. Green "Execute Disbursement" button on Wire Pack with confirmation dialog + amber gate-warnings for soft failures. Live progress card shows each intent through the pipeline. PaymentsCover replaced empty state with real intents table including summary cards + Retry button on failures. Verified end-to-end: 1 wire executed → 4+ audit events written → settled with mock provider ref. Swap in a real bank API later by replacing `simulateOne()` only. |
| **HIGH** | `src/components/pivt-complete/cover/CapTableCover.tsx` + `DocumentsCover.tsx` | `b7571ee` | **Fixes the cap-table upload that secretly did nothing.** CapTableCover uploaded files to storage + inserted a `contract_documents` row, then ran a 2-second `setTimeout` *pretending* AI extraction was happening — `document-ai` was never invoked, so `cap_table_entries` never got populated. Now CapTableCover invokes `document-ai` for real (reads CSV content client-side so the classifier has actual data), which chains into `deal-workflow-orchestrator.processCapTable()` and upserts equity holders. Plus `DocumentsCover` now polls every 3s while any doc is in-flight so the status badge auto-flips from "Processing" to "Processed" without a manual reload (auto-stops once all docs terminal). |
| **Medium** | `src/pages/VerifyPage.tsx` | `f1c0352` | **Counterparty KYC flow now survives reloads.** The `/verify?token=…` page where invited stakeholders complete KYC/KYB lost everything on browser refresh — uploaded blobs sat orphaned in `verification-documents` with no DB record (those only land on submit via `verify-token`), and all form state was wiped. For a real customer doing a 3-step submission with several uploads, that's a brutal UX. Now writes per-token draft to localStorage on every change, restores on mount with a friendly toast, clears on successful submit. No new edge functions or schema needed. |
| **Low** | `src/hooks/useDealDocuments.ts` (new) + `DocumentsCover.tsx` doc note | `76698bc` | **Unifies the two doc-table read path.** The repo has both `deal_documents` (user uploads, richer fields) and `contract_documents` (doc-ingestion pipeline writes + CapTableCover / DealDocumentUploader uploads) — coexisting for historical reasons. DocumentsCover already merged them inline but the logic wasn't reusable. New `useDealDocuments(dealId)` hook normalizes both into a single `UnifiedDocument[]` shape (folds status enums, filename/file_name, file_url/file_path), dedupes by id, sorts most-recent-first, and supports optional polling while in-flight. Includes a `source` field so callers can route deletes/updates back to the correct table. Future surfaces that need "all docs for this deal" use the hook instead of duplicating the merge. Read-side only — full schema consolidation would be a much larger lift with downstream edge-function impact. |
| **HIGH** | `src/pages/DPAPage.tsx` (new) + `LoginPage.tsx` + `AuthPage.tsx` + `App.tsx` + `PIVTCompleteUnified.tsx` + `EMAIL_INFRASTRUCTURE.md` (new) | `3e842e3` | **Customer-readiness blockers — legal + email.** New 17-section Data Processing Agreement at `/dpa` (GDPR Art. 28 / UK GDPR / CCPA shape; covers sub-processors, security measures, 72h breach notification, EU SCCs, data-subject rights, return/deletion). Required agreement checkbox at signup linking to Terms / Privacy / DPA — disables Create Account until checked. Acceptance is stamped on `auth.users.raw_user_meta_data` (`terms_accepted`, `terms_accepted_at`, `terms_version: '2026-05-18'`) and logged as an authTrackingService event so support can prove which version a user agreed to. Always-visible legal footer below the login card. DPA link added to the main app footer. Plus a new EMAIL_INFRASTRUCTURE.md documenting the two production email pipelines (Lovable email-js for auth flow at `notify.pivttech.ai`, Resend direct API for KYC invites via `RESEND_API_KEY`), what's verified working, and what's still missing for full customer readiness (test-email button, bounce webhooks, email preferences). |
| **HIGH** | `src/pages/NotFound.tsx` + `src/App.tsx` + `src/components/pivt-complete/cover/AccountSettingsPanel.tsx` (new) + `SettingsCover.tsx` + `PRODUCTION_CHECKLIST.md` (new) | `6f66ded` | **Customer-readiness quick wins.** (1) Branded 404 page with bad-path display + Back-home / Go-back / Contact-support actions + common-destinations grid + legal footer; moved out of AuthGuard so signed-out users hitting stale links see it (was being silently redirected to /login). (2) New **Account tab** in Settings (first tab, default-selected): full TOTP 2FA enrollment via `supabase.auth.mfa.*` (QR + manual secret + verify); GDPR/CCPA-aligned **Export my data** button downloads a JSON bundle of audit_log + deal_comments + deal_participants + cap_table_entries; **Delete account** flow with email-type-to-confirm dialog that records the request to audit_log + signs the user out (purge is admin-manual until a delete edge fn ships); admin-only **Send test email** button that triggers a password-reset to the admin's own address through the Lovable pipeline for delivery isolation. (3) New `PRODUCTION_CHECKLIST.md` at repo root with every external-dashboard toggle needed before opening to paying customers (Supabase Confirm Email — currently OFF, DNS/DKIM/SPF/DMARC, secrets rotation, backups, observability, pre-launch cleanup). |
| **HIGH** | `src/components/onboarding/OnboardingWizard.tsx` (new) + `PIVTCompleteUnified.tsx` + `DealsCover.tsx` + `AccountSettingsPanel.tsx` | `ab1ccca` | **First-run onboarding wizard.** 4-step modal auto-opens for any user whose `auth.user_metadata.onboarding_complete` isn't true (which is everyone today). Steps: Welcome with personalised greeting + 3-emoji intro; capture firm_name + role (7 presets + Other, both optional); two-card path picker (Create my first deal vs Show me a demo first); confirmation with contextual message + Newton tip. On finish writes `onboarding_complete + onboarding_completed_at + onboarding_version + onboarding_choice + firm_name + role` to user metadata and an `onboarding_completed` audit_log breadcrumb. Real path dispatches a new `pivt:open-create-deal-modal` event that DealsCover listens for, popping the existing New Deal form. Demo path queries `deals.is_demo=true` + routes the user to the first match. Idempotent — does NOT auto-reopen once `onboarding_complete=true`. Account tab gained a 'Restart tour' button that dispatches `pivt:open-onboarding` so support can demo it to teammates. |
| **REQUIRES MIGRATION** | `supabase/migrations/20260519010000_multi_tenancy_phase_1.sql` (new) + `src/contexts/OrgContext.tsx` (new) + `src/components/pivt-complete/OrgSwitcher.tsx` (new) + `App.tsx` + `PIVTCompleteUnified.tsx` + `MULTI_TENANCY_DEPLOY.md` (new) | `5e46c2b` | **Multi-tenancy Phase 1.** Adapts to the existing `organizations` + `organization_memberships` tables (already in repo migrations but empty) — does not duplicate. Migration adds `slug / legal_entity_name / billing_email / org_type / created_by / updated_at` to organizations, adds `deals.org_id`, helper RLS functions (`user_org_ids`, `has_org_role`, `demo_org_id`), backfills one personal org per existing user + a shared 'PIVT Demo' org, additive SELECT policy on deals scoped by org membership. Frontend: OrgProvider with active-org persistence in localStorage and graceful "schema not deployed" detection; topbar OrgSwitcher with workspace dropdown, demo entry, create-new-workspace dialog; renders an amber "Workspace setup needed" pill when schema is partial. See MULTI_TENANCY_DEPLOY.md for the paste-into-Studio deployment step that activates everything. **Deployed via Lovable Cloud SQL editor 2026-05-19; verified live with 9 orgs created.** |
| **HIGH** | `useDealOperations.ts` + `DealsCover.tsx` + `OnboardingWizard.tsx` + `WorkspaceSettingsPanel.tsx` (new) + `SettingsCover.tsx` | `aa1bc14` | **Multi-tenancy Phase 2** — closes the Phase-1 loop so the workspace switcher actually does what it implies. (1) Deals list filters by active workspace via `deals.org_id`. Demo workspace shows demo deals; customer workspace shows only its deals. Verified: switching between PIVT Demo (3 deals) and PIVT Tech (0 deals) correctly isolates data. (2) Create Deal injects `org_id` from active workspace so new deals land in the right org; blocks creation when active workspace is the demo org (humane toast pointing to switcher). (3) OnboardingWizard auto-creates a workspace from `firm_name` when a new signup finishes (Phase 1's backfill only covered existing users at deploy-time). (4) New **Workspace** tab in Settings: rename (admins+), members list with role badges + your-role highlight, leave workspace with only-owner protection guard. |
| **REQUIRES MIGRATION** | `supabase/migrations/20260519020000_org_invites.sql` (new) + `WorkspaceSettingsPanel.tsx` + `AcceptInvitePage.tsx` (new) + `App.tsx` | `cc508c0` | **Multi-tenancy Phase 3 — invite + accept.** Migration adds `organization_invites` table with token + 14d expiry + role + accepted/revoked timestamps, unique partial index preventing duplicate pending invites per `(org_id, email)`, RLS (admins manage; tokens are addressable by anyone authenticated for the accept page), and a SECURITY DEFINER `accept_organization_invite(uuid)` RPC that's atomic + idempotent. WorkspaceSettingsPanel gained: working **Invite teammate** button (admins, non-demo) → dialog with Editor/Viewer role select with help captions → creates invite, auto-copies share link, "Invite another" shortcut. Pending invites section with email + role + expiry-countdown + Copy-link + Revoke buttons. Duplicate-invite attempt surfaces a humane "revoke the existing one first" toast. New `/accept-invite?token=…` page validates token + shows org name + role + expiry; handles loading/ready/expired/revoked/used/not-found/schema-missing states with tailored panels; signed-out users get a signup CTA with `?next=` redirect; signed-in users with a different email get an "Accept anyway" warning. Audit-log breadcrumbs on every create + revoke. **Deployed via Lovable Cloud SQL editor 2026-05-19 + verified end-to-end live:** create-invite → link → accept-page render → RPC accept → idempotent re-accept → duplicate guard → revoke → re-invite all pass. Email-sending (auto-send via Resend) deferred to a future pull; the admin shares the link manually for now. |
| **HIGH** | `src/components/newton/NewtonStepChips.tsx` (new) + `src/services/newtonActionService.ts` + `DealWorkspaceCover.tsx` | `44c5796` | **Inline Newton chips on every workspace step.** Audit found Newton had 32+ backend actions wired but none surfaced in the per-step UI — every interaction required cmd-J / sparkle button into the chat. Now each step renders a "Newton can…" bar with context-specific action chips (Overview: Summarize readiness + List blockers + Suggest next steps + Predict closing; Stakeholders/KYC: Generate KYC/KYB; Deal Inputs: Parse funds flow + Run discrepancy check; Verification: Run discrepancies + Summarize closing risks; Approvals: Prepare package + Predict delays; Execution: Summarize risks + Draft closing certificate; Audit: Summarize risks; Comments: Suggest next actions). One-tap fires the real backend action; result lands in an expandable markdown card directly below the chip row. Success cards auto-dismiss after 12s; errors stick. Sub-step gating so chips only appear on the right tab. `callNewtonAction` exported from the service so chips invoke any action by name without per-action wrappers. |

## How to apply one

When you're ready to update any of these in live:

1. Tell me which file and I'll paste its full new content (or a focused diff if the file is large), the same way I did for `require-jwt.ts` and `newtonActionService.ts`.
2. Paste the change into Lovable's code editor at the file's path.
3. Save. Lovable commits to `main` and redeploys the frontend (usually under a minute).

## How to find the pattern in the file you're pasting into

Each file currently has a `fetch(..., { headers: { Authorization: \`Bearer \${...ANON_KEY}\` } })` shape. The fix replaces that with the user JWT pattern:

```ts
const { data: { session } } = await supabase.auth.getSession();
const bearer = session?.access_token || ANON_KEY;
// then: Authorization: `Bearer ${bearer}`,  apikey: ANON_KEY
```

A few files also need `import { supabase } from '@/integrations/supabase/client';` at the top if not already imported.

## When this list is empty

When all five are pasted (or after a future `dev` → `main` merge handles them all in one go), delete this file. It's a temporary index, not part of the product architecture.
